### THIS FILE IS MANAGED BY PUPPET. ANY MANUAL CHANGES WILL GET OVERWRITTEN.

check process ntpd with pidfile /var/run/ntpd.pid
   start program = "<%= scope.lookupvar('ntp::params::service_start') %>"
   stop program = "<%= scope.lookupvar('ntp::params::service_stop') %>"
   alert <%= scope.lookupvar('ntp::monit::monitor_email') %> with reminder on 480 cycles

